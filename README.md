# python402
bitcoin / crypto / rpc python projects

### just a simple http server
`python3 -m http.server 81`

Test it with:
`curl http://localhost:81`

##### mac
```sh
python3 --version
pip3 --version
exit()
```
##### unix
```sh
python3 --version
pip3 --version
```
